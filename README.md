# Anime Japanese Learning System

系统版本：v3

一套面向 Codex 对话使用的番剧日语口语学习提示词文件包。

这个项目不是自动化 App，也不是字幕翻译工具。它的目标是让你每次把番剧字幕交给 Codex 时，Codex 都能按照稳定规则筛选台词、判断现实可用度、设计口语练习，并把你的学习状态沉淀到可迁移的全局口语记忆里。

## 快速开始：推荐开场白

在新的 Codex 对话中，请先把本项目文件下载到当前工作区，或把这些 Markdown 文件上传给 Codex。系统会优先在当前工作区建立工作副本，之后只修改工作区副本，不动下载目录、上传附件或 GitHub 原始模板。

```text
AGENTS.md
01-学习偏好.md
02-学习流程.md
03-错因日志.md
04-复习队列.md
05-已习得库.md
```

然后复制下面这段话作为第一条消息：

```text
请先把我提供的 AGENTS.md、01-学习偏好.md、02-学习流程.md、03-错因日志.md、04-复习队列.md、05-已习得库.md 复制到当前工作区作为工作副本；如果当前工作区已经有同名文件，不要覆盖。之后请只读取和修改当前工作区副本，并严格按照这个番剧日语学习系统运行。然后请进入学习系统自我介绍模式。
```

如果你已经准备好字幕，可以在第一条消息后继续上传字幕文件，并补充番剧名和集数。

## v3 更新声明：迁移、维护与模式化使用

v3 在 v2 的多番剧并行基础上，增加了系统生命周期管理：旧版迁移、资源维护、学习清单转写、自我介绍和统计总结。

本次更新的重点：

- 增加明确模式触发词，例如“现在进入学习迁移模式”“进入资源维护模式”“进入学习清单转写模式”“自我介绍”。
- 增加 v1/v2 到新版的迁移流程：旧 `05` 可直接保留，缺失字段用保守默认值补齐。
- 增加资源维护机制：将 `03` 的细错误、`04` 的候选任务压缩沉淀到 `05`，防止信息越来越多。
- 增加“建议不等于同意”原则：系统建议维护或迁移后，如果用户没有明确确认，默认不同意。
- 将 `05` 的“快速索引”改为更轻的“快速路由索引”，减少与正文重复。
- 增加学习清单转写模式：把 AI 友好的表达簇转换成人类可读学习清单，但不改写原始 `05`。
- 增加低频统计总结：每 3 次有效学习、资源维护后、阶段结束后，适度反馈学习进展。

## v2 更新声明：多番剧并行与对话式动态复习

v2 将系统从“单番剧学习项目”升级为“多番剧输入下的全局口语学习系统”。

现在，多部番剧可以共用同一套学习记忆。番剧只是素材来源，不是知识边界；表达簇、错因、复习候选和掌握进度都服务于用户整体日语口语能力。

本次更新的重点：

- `03-错因日志.md` 和 `04-复习队列.md` 在一个学习周期内跨 Codex 新对话保留，直到用户手动清空。
- `04-复习队列.md` 不再是每日任务表，而是动态复习候选池。
- `05-已习得库.md` 是全局真实进度来源，记录表达簇、变体、来源和复习状态摘要。
- 同一表达簇跨番剧出现时，合并来源和复习进度，不创建重复条目。
- 复习算法不套用 Anki 固定间隔，而是在每次对话中根据当前字幕、错因、证据强度和认知负担选择少量复习项。
- 系统通过快速路由索引和低精度事件记录节省 token：不记录每次普通引用，只记录讲解、练习、回答、答错、用户声明、新变体和旧表达复现等学习事件。

## 使用场景

1. 从 GitHub 下载本项目的 Markdown 文件。
2. 在 Codex 里新开一个对话，把这些文件和字幕文件一起上传。
3. Codex 按文件中的规则进行教学。
4. 教学结束后，保留本学习周期更新后的 `03-错因日志.md`、`04-复习队列.md` 和 `05-已习得库.md`。
5. 下一次学习时，重新下载原始文件，再把当前学习周期的 `03/04/05` 合并进去。

这样，每次新对话仍然能轻量启动，但你的错因、复习候选和长期掌握进度会持续累积。

## 常用模式关键词

| 你说 | 系统进入 |
|---|---|
| 上传字幕 / 开始学习 | 普通学习模式 |
| 复习一下 / 今天只复习 | 复习模式 |
| 现在进入学习迁移模式 | 学习迁移模式 |
| 进入资源维护模式 | 资源维护模式 |
| 进入学习清单转写模式 | 学习清单转写模式 |
| 总结一下最近进展 | 统计总结模式 |
| 自我介绍 / 你有哪些模式 | 自我介绍模式 |

迁移、维护、清理、删除、压缩和大规模合并都需要用户明确确认。系统的建议不代表用户已经同意。

## 文件结构

```text
.
├── AGENTS.md
├── 01-学习偏好.md
├── 02-学习流程.md
├── 03-错因日志.md
├── 04-复习队列.md
├── 05-已习得库.md
└── .gitignore
```

## 每个文件的作用

| 文件 | 作用 | 是否继承 |
|---|---|---|
| `AGENTS.md` | 定义 Codex 的身份、规则、文件更新逻辑 | 通常不变 |
| `01-学习偏好.md` | 记录长期学习偏好 | 通常不变 |
| `02-学习流程.md` | 记录字幕处理和练习流程 | 通常不变 |
| `03-错因日志.md` | 记录当前学习周期的全局错因 | 周期内继承 |
| `04-复习队列.md` | 记录当前学习周期的动态复习候选池 | 周期内继承 |
| `05-已习得库.md` | 记录跨番剧、跨对话的表达簇和真实掌握进度 | 长期继承 |

## 独特设计

### 1. 表达簇，而不是句子列表

日语里同一个意思可能有很多外形：普通形、丁寧形、敬语、缩略、方言、角色口癖等。

所以 `05-已习得库.md` 不把知识记录成孤立句子，而是记录成“表达簇”。例如同一个“请求许可”的核心表达，可以包含：

- `〜てもいい？`
- `〜てもいいですか`
- `〜てもええ？`
- `〜ても大丈夫？`

这样，当你已经掌握核心表达时，系统不会因为换了礼貌形或方言就把它误判成全新知识。

### 2. 用户声明优先

Codex 并不总能从一次对话里准确判断你真实掌握了什么。因此规则明确规定：

如果你说“这个我已经懂了”“这个我会说”“这个只是关西说法，我懂”，系统必须把这视为状态更新证据，而不是闲聊。

这让知识库可以被你主动校准，避免系统反复教学你已经会的内容。

### 3. 静默练习默认规则

很多时候，Codex 最后让你练一句话，你会看完并练习，但不会继续回复。

本项目默认：如果系统最后布置了练习，而你没有继续回复，就视为你完成了一次静默练习。但状态更新必须保守，最多只能标为 `接触`，不能自动升级为掌握。

这既符合真实使用习惯，也避免夸大学习结果。

### 4. 多番剧并行

传统番剧学习很容易变成：

```text
看一部番 → 记一些句子 → 换一部番 → 重新开始
```

这个项目的目标是：

```text
多部番剧 → 筛选表达 → 练习输出 → 写入全局表达簇 → 合并复习进度
```

多部番剧之后，知识库会记录：

- 你已经掌握哪些核心功能。
- 哪些表达只是新变体。
- 哪些说法适合现实中主动使用。
- 哪些只需要听懂。
- 哪些内容下次不用重复教。

同一表达簇在不同番剧中出现时，系统会追加来源记录并合并复习进度；如果字面相同但功能、语气或关系距离不同，则拆成不同表达簇。

### 5. 对话式动态复习

`04-复习队列.md` 不是每日任务表。它是 Agent 在每次对话前查看的动态候选池。

有新字幕时，系统只插入 1-3 个与当前字幕相关的旧表达复习；没有新字幕、用户想复习时，再从候选池中选择 3-5 个重点。复习选择会考虑：

- 当前字幕相关度。
- 历史错因。
- 证据强度。
- 待验证点。
- 现实可用度。
- 当前对话负担。
- 用户声明掌握程度。

### 6. 节省 Token 的快速路由索引和轻量事件记录

`05-已习得库.md` 设计了“快速路由索引”。

Codex 每次处理字幕时，先看快速路由索引，只在无法判断时再展开详细条目。这样可以减少每次对话读取整份知识库的成本，尤其适合知识库逐渐变大后的长期使用。

快速路由索引只记录表达簇ID、检索键、主功能、路由、状态和备注。正文表达簇仍然是事实来源；如果二者冲突，以正文为准。

系统也不会记录每次普通引用，只记录真正有学习意义的事件，例如正式讲解、布置练习、用户回答、答错、用户声明、新变体和旧表达复现。

### 7. 资源维护漏斗

系统长期使用后，信息会按以下方向压缩：

```text
03 原始错误观察 -> 04 可行动复习候选 -> 05 长期掌握摘要
```

资源维护模式可以保留、合并、完成、压缩或删除候选项，但必须先给方案并等待用户确认。

### 8. 学习迁移

旧版用户可以说：

```text
现在进入学习迁移模式
```

然后上传旧版 `05-已习得库.md`，可选上传旧版 `03/04`。系统会保留已有表达簇和来源记录，为缺失的复习状态摘要补保守默认值，并输出迁移报告。

## 推荐工作流

1. 新对话开始时，上传本项目文件。
2. 如果你已经有当前学习周期更新后的 `03/04/05`，用它们替换原始版本。
3. 上传本集字幕。
4. 让 Codex 按 `AGENTS.md` 开始教学。
5. 教学结束时，Codex 会优先直接把状态更新写回当前工作区里的 `03/04/05`；如果文件不可写，才会输出可复制的更新片段。
6. 当一个学习周期结束后，可以手动清空 `03/04`，但保留 `05` 作为长期资产。

## 适合谁

这个项目适合：

- 想用番剧练日语口语的人。
- 不想逐句翻译，而想筛选现实可用表达的人。
- 同时看多部番剧，希望共用一套口语记忆的人。
- 希望 Codex 在新对话里仍然“记得自己学过什么”的人。
- 希望节省上下文和 token，而不是每次重新解释学习偏好的人。

## 注意

本项目依赖 Codex 对上传文件的阅读和执行能力。它本身不包含自动下载字幕、自动同步 GitHub 或自动合并知识库的脚本。

`03/04/05` 的迁移和合并目前设计为手动完成，这是为了保持系统轻量、透明，并避免自动化过度复杂。
